#include "RadioBackend.h"

// RadioBackend is an abstract interface whose members are defined inline in the
// header.  This translation unit exists to anchor AUTOMOC generation for the
// Q_OBJECT (moc_RadioBackend.cpp) and to give the class a home in the build.
